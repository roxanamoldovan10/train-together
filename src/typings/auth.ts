export const enum authGetters {
  GetUserAuthState = 'getUserAuthState',
}

export const enum authMutations {
  SetUserAuthState = 'setUserAuthState',
}

export const enum authActions {
  AuthentificateUser = 'authentificateUser',
  Logout = 'logout',
}
